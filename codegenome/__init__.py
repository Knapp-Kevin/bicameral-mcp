# codegenome/ — adapter boundary, identity records, evidence packets.
#
# Phase 1+2 (#59): adapter ABC, deterministic identity, config flags,
# bind-time identity writes. All disabled by default (CodeGenomeConfig
# defaults every flag to False).
